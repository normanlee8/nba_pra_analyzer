The comprehensive updates across the system address critical mathematical errors and logical flaws to ensure near 100% accuracy for S-Tier and A-Tier props. A primary fix involved correcting a formula in the probability generator that was double-subtracting push probabilities on whole-number lines, which previously depressed win probabilities and caused the system to miss high-confidence opportunities. The inference engine was further refined by implementing a "volatility filter" for Under bets, removing a previous bypass that allowed high-variance players to receive top grades; this ensures that only predictable, low-ceiling outcomes are graded as S-Tier. To eliminate target leakage and "lookahead bias," the Defense vs. Position (DvP) and Form Ratio calculations were rebuilt to use strictly expanding rolling averages, preventing future game data from artificially inflating the model's historical accuracy. Furthermore, the system now requires a minimum "delta gap" or expected value (EV) for S-Tier props, ensuring that a significant mathematical cushion exists between the projection and the sportsbook line. Finally, the model training was sharpened by anchoring sample weights to the specific dates within cross-validation folds and removing manual post-inference bias corrections, allowing the meta-ensemble to calibrate itself natively.