 Gemini said

The updates implemented across the repository modernize the system's mathematical foundations and remove legacy heuristics that were prone to error. By centralizing risk thresholds in config.py, the system now ensures that tiering decisions are consistent across all modules, eliminating the previous disconnect between intended and actual classification logic. The core inference engine in inference.py was refined to include a dynamic system bias correction, which allows the model to learn from its own recent misses and adjust projections accordingly, directly solving the issue of unaddressed model drift. Additionally, the feature engineering in generator.py was upgraded to replace arbitrary "magic number" usage shifts with a proportional distribution model based on active player footprints, while blowout potential was transitioned to a non-linear scale to more accurately reflect the risk starters face in lopsided games. Finally, the "Under" pick logic in calculator.py and inference.py was adjusted to account for the mathematical reality that high-volatility players are often safer to bet against, and combo prop variance was bolstered with a higher correlation floor to prevent overestimating win probabilities.